---

# Vivo Live Photo 管理工具 - 产品需求文档（PRD）

## 1. 项目名称（暂定）

**Live Manager**

一个专门用于管理 vivo Live Photo 的轻量级 Android 工具。

---

# 2. 项目背景

vivo 手机的 Live 图片功能采用特殊存储方式：

同一目录下保存：

```
IMG_xxx.jpg
IMG_xxx.mp4
```

其中：

- `.jpg` 是静态照片；
- `.mp4` 是 Live 图片对应的动态视频。

vivo 原生相册可以识别二者关系，并显示为一张 Live 图片。

但是第三方相册（例如 Google 相册）通常无法识别这种关联，会导致：

- JPG 和 MP4 分开显示；
- 图片和视频混在时间线中；
- 多张连续拍摄时难以判断 JPG 对应哪个 MP4；
- 管理和清理非常困难。

本项目目标：

开发一个**小而美的 Live 图片管理工具**，提供接近原生相册的浏览体验，同时提供针对 Live 视频的管理能力。

---

# 3. 产品定位

## 不是：

❌ 一个完整相册替代品  
❌ Google Photos 替代品  
❌ 图片编辑软件  

## 而是：

✅ vivo Live Photo 专用管理工具

核心价值：

> 正确识别 vivo Live 图片结构，以“一个 Live 图片对象”的形式展示和管理，而不是把 JPG 和 MP4 当成两个独立媒体。

---

# 4. 第一版本目标（MVP）

第一版只实现以下功能：

## 核心功能

### 4.1 自动识别 Live 图片

扫描用户照片目录：

主要：

```
DCIM/Camera
```

识别规则：

如果：

```
xxx.jpg
xxx.mp4
```

存在，并且：

- 文件名完全一致（除扩展名）
- MP4 时长符合 Live 图片特征（建议 < 5 秒）

则认为：

```
xxx.jpg + xxx.mp4
```

属于一个 Live Photo。

内部模型：

```
LivePhoto
{
    imagePath: jpg路径,
    videoPath: mp4路径,
    createTime,
    imageSize,
    videoSize
}
```

---

# 5. UI / 交互要求

## 5.1 首页：Live 图片列表

要求：

类似手机原生相册。

展示：

- 网格布局；
- 图片缩略图；
- 不显示 MP4；
- 每个 Live 图片只显示一次。


示例：

```
--------------------------------

[图片] [图片] [图片]

 LIVE   LIVE   LIVE


--------------------------------

共发现：
328 张 Live 图片

占用空间：
1.5GB
```

---

## 5.2 长按预览（核心功能）

用户长按某个 Live 图片：

行为：

1. 默认显示 JPG 静态图片；
2. 长按开始播放对应 MP4；
3. 松手停止播放；
4. 返回静态图片。

效果类似：

- vivo 原生动态照片；
- iPhone Live Photo。

要求：

- 动画流畅；
- 不跳转视频播放器；
- 不显示视频控制栏。

---

## 5.3 图片详情页

点击图片进入详情。

显示：

基础信息：

```
文件名
拍摄时间
图片大小
视频大小
总占用空间
```

示例：

```
IMG_20260805_182516

照片:
2.2 MB

动态:
3.8 MB

总计:
6 MB
```

---

# 6. 删除功能

## 6.1 删除动态部分

用户选择：

```
删除动态视频
```

行为：

删除：

```
xxx.mp4
```

保留：

```
xxx.jpg
```

结果：

Live 图片变成普通静态照片。

---

## 6.2 删除方式要求

必须类似 Android 原生相册：

不要直接：

```
File.delete()
```

要求：

使用 Android MediaStore 删除机制。

目标：

进入系统回收站（如果系统支持）。

用户可以：

- 恢复；
- 永久删除。

---

# 7. EXIF 功能

## 第一版本：

支持查看 EXIF。

显示：

例如：

```
相机：
vivo X200 Pro mini

焦距：
25mm

ISO：
1741

快门：
1/20

曝光：
-2.7

GPS:
上海
```

---

## 后续版本：

支持修改：

- 拍摄时间；
- GPS；
- 描述；
- 其他 EXIF 字段。

注意：

修改 JPG EXIF 时，需要保证：

- 不破坏 Live 图片关联；
- 不影响 JPG + MP4 配对关系。

---

# 8. 非功能要求

## 8.1 隐私

必须：

- 本地运行；
- 不上传图片；
- 不需要账号；
- 不需要服务器。

---

## 8.2 性能

目标：

- 支持几千张照片；
- 快速扫描；
- 缩略图缓存；
- 滚动流畅。

---

## 8.3 UI 风格

要求：

“小而美”。

特点：

- 简洁；
- 接近系统相册；
- 不堆功能；
- 无广告；
- 无复杂设置。

---

# 9. 技术方案建议

## 开发环境

使用：

```
VS Code
+
Flutter
+
Android SDK
```

原因：

- 支持快速 UI 开发；
- 支持 Hot Reload；
- 方便实时查看效果。

---

# 10. 推荐技术架构

## Flutter 层

负责：

- UI；
- 图片网格；
- 手势；
- 页面交互。

---

## Android 原生桥接层（Kotlin）

负责：

- MediaStore；
- 系统回收站；
- EXIF 高级操作；
- 文件管理权限。

结构：

```
Flutter UI

     |
     |
Platform Channel

     |

Kotlin Android

     |

MediaStore
ExifInterface
```

---

# 11. 第一版禁止范围

为了保持项目简单，第一版不要实现：

❌ 云同步  
❌ AI分类  
❌ 人脸识别  
❌ 图片编辑  
❌ 视频编辑  
❌ 社交分享  
❌ 在线功能  

---

# 12. 验收标准

第一版完成后：

用户可以：

1. 打开 App；
2. 自动看到所有 vivo Live 图片；
3. 浏览体验类似原生相册；
4. 长按图片播放动态效果；
5. 选择某张 Live 图片；
6. 删除对应 MP4；
7. JPG 保留；
8. 删除行为进入系统回收站；
9. 查看照片 EXIF 信息。

---

# 13. 后续可能扩展

未来版本：

- 批量删除 Live 视频；
- 显示可释放空间；
- Live 转普通照片；
- 支持 OPPO/Xiaomi/Samsung Motion Photo；
- EXIF 批量编辑；
- 导出备份。

---

# 总结

这是一个：

> **针对 vivo Live Photo 双文件结构的轻量级本地媒体管理工具。**

核心不是做相册，而是：

**正确识别 Live 图片 → 原生体验浏览 → 精准管理动态视频。**

开发时应优先保证：

1. Live 图片识别准确；
2. 浏览体验接近原生相册；
3. 长按播放流畅；
4. 删除安全（进入回收站，不误删照片）。

---
